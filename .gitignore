# Root-level ignores (docker-compose.yml and README.md live outside the
# backend/ and frontend/ git repos; this file is for when the root itself
# is version-controlled, e.g. as a meta-repository).

# Environment files (secrets)
.env
.env.*
!.env.example

# Docker Compose local overrides
docker-compose.override.yml

# Logs
*.log

# Editor / OS
.idea/
.vscode/
.DS_Store
*.swp
Thumbs.db
/backend
/frontend